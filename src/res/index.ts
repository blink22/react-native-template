import images from "./images"
import colors from "./colors"
import fonts from "./fonts"
import strings from "./strings"
import typography from "./typography"

export const R = { images, colors, fonts, strings, typography }
